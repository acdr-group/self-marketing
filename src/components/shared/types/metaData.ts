export interface MetaData {
    author: string
    releaseInfo: {
        version: string
        packager: string
        "runtime engine": string
        "first release date": string
        category: string
    }
}